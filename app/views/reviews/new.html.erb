<div class="container">
    <%= simple_form_for([@flat, @review]) do |f| %>
        <%= f.input :comment %>
        <%= f.input :rating, collection: 1..5, selected: 5 %>>
        <%= f.submit %>
    <% end %>
</div>
